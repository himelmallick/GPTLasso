% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper.r
\name{gen_simmba_multistudy}
\alias{gen_simmba_multistudy}
\title{Generate simulated multi-study, multiview data for benchmarking}
\usage{
gen_simmba_multistudy(
  nsample,
  nstudy = 1,
  snr = 1,
  p.train = 0.7,
  de.prob = rep(0.1, 3),
  de.downProb = rep(0.5, 3),
  de.facLoc = rep(1, 3),
  de.facScale = rep(0.4, 3),
  ygen.mode = c("LM", "Friedman", "Friedman2"),
  outcome.type = c("continuous", "binary", "survival"),
  surv.hscale = 1,
  cens.lower = 1,
  cens.upper = 3,
  rho.beta = 1,
  sigma.alpha = 0,
  tau.snr = 0,
  nrep = 100,
  seed = 1234
)
}
\arguments{
\item{nsample}{Number of samples per study.}

\item{nstudy}{Number of studies.}

\item{snr}{Signal-to-noise ratio for continuous outcomes.}

\item{p.train}{Train-test split ratio.}

\item{de.prob}{Differential-expression probability across all modalities.}

\item{de.downProb}{Down-regulation probability across modalities.}

\item{de.facLoc}{Differential-expression factor location across modalities.}

\item{de.facScale}{Differential-expression factor scale across modalities.}

\item{ygen.mode}{Outcome-generation mode: \code{"LM"}, \code{"Friedman"}, or \code{"Friedman2"}.}

\item{outcome.type}{Outcome type: \code{"continuous"}, \code{"binary"}, or \code{"survival"}.}

\item{surv.hscale}{Multiplicative scale on hazard for survival outcomes.}

\item{cens.lower}{Lower bound for uniform censoring time.}

\item{cens.upper}{Upper bound for uniform censoring time.}

\item{rho.beta}{Shared-versus-study-specific mixing level in \verb{[0, 1]}.}

\item{sigma.alpha}{Standard deviation of study-specific intercepts.}

\item{tau.snr}{Standard deviation of the log signal-to-noise ratio across studies.}

\item{nrep}{Number of repetitions.}

\item{seed}{Random seed.}
}
\value{
A list containing:
\itemize{
\item \code{trainDat}: list of simulated training datasets indexed by repetition, then study.
\item \code{testDat}: list of simulated testing datasets indexed by repetition, then study.
\item \code{true_betas}: shared and study-specific coefficient vectors used to generate the outcomes.
\item Simulation settings echoed back, including \code{snr}, \code{p.train}, \code{de.prob},
\code{de.downProb}, \code{de.facLoc}, \code{de.facScale}, \code{nrep}, \code{seed}, \code{ygen.mode},
\code{outcome.type}, \code{nstudy}, \code{rho.beta}, \code{sigma.alpha}, and \code{tau.snr}.
}
}
\description{
Generates multi-study simulated multiview datasets with specified parameters,
including per-study sample size, signal-to-noise ratio, differential
expression probabilities, and response generation mode. The output is split
into training and testing sets within each study.
}
\details{
The model uses a shared and study-specific coefficient structure:
for study \code{s},
\code{beta_s = sqrt(rho.beta) * beta_shared + sqrt(1 - rho.beta) * beta_indiv_s},
where \code{beta_shared} is common across studies and \code{beta_indiv_s} is
study-specific.

Special cases:
\itemize{
\item \code{nstudy = 1}, \code{rho.beta = 1}, \code{sigma.alpha = 0}, \code{tau.snr = 0} recovers
the single-study generator behavior.
\item \code{rho.beta = 0} yields independent study-specific coefficients.
}
}
