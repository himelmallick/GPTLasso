% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.r
\name{predict.gptLasso}
\alias{predict.gptLasso}
\title{Predict from a fitted multistudy multiview gptLasso model}
\usage{
\method{predict}{gptLasso}(
  object,
  xtest,
  ytest = NULL,
  target = NULL,
  type = c("link", "response", "class"),
  overall.lambda = c("lambda.min", "lambda.1se"),
  ind.lambda = c("lambda.min", "lambda.1se"),
  pre.lambda = c("lambda.min", "lambda.1se"),
  return.link = FALSE,
  ...
)
}
\arguments{
\item{object}{A fitted \code{gptLasso} object.}

\item{xtest}{A named list with \code{feature_table}, \code{sample_metadata}, and \code{feature_metadata}.}

\item{ytest}{Optional list of response vectors used to compute performance summaries.}

\item{target}{Optional target study/studies for prediction. Default \code{NULL}
uses the target configuration stored in \code{object} (or all studies for legacy
fits). When supplied, must be a character vector of training study names.
\code{xtest} studies must be a subset of the resolved target studies.}

\item{type}{Prediction scale: \code{"link"}, \code{"response"}, or \code{"class"} for binomial fits.}

\item{overall.lambda}{Lambda rule used for overall-model prediction.}

\item{ind.lambda}{Lambda rule used for individual-model prediction.}

\item{pre.lambda}{Lambda rule used for pretrained-model prediction.}

\item{return.link}{Should link-scale predictions also be returned?}

\item{...}{Reserved for future extensions.}
}
\value{
A \code{predict.gptLasso} object, stored as a list. Important components include:
\itemize{
\item \code{yhatoverall}: study-wise predictions from the pooled stage-one fit.
\item \code{yhatind}: study-wise predictions from the individual study fits.
\item \code{yhatpre}: study-wise predictions from the pretrained transfer fits.
\item \code{supoverall}: selected feature indices from the pooled stage-one fit.
\item \code{supind}: union of selected feature indices across the individual fits.
\item \code{suppre.common}: stage-one support reused by the pretrained fits.
\item \code{suppre.individual}: additional feature indices selected by the pretrained fits beyond \code{suppre.common}.
\item \code{linkoverall}, \code{linkind}, \code{linkpre}: optional link-scale predictions when \code{return.link = TRUE}.
\item \code{metrics}: optional held-out performance summaries when \code{ytest} is supplied. The primary metric is stored in \code{metrics$MSE}, \code{metrics$AUC}, or \code{metrics$deviance} as a \verb{3 x (k + 2)} table with rows \code{overall}, \code{ind}, and \code{pre}, and columns \code{pooled}, \code{mean}, plus one column per study. Gaussian fits also include \code{metrics$r2} with the same layout.
\item \code{erroverall}, \code{errind}, \code{errpre}: backward-compatible performance summaries when \code{ytest} is supplied.
\item \code{target}: the effective target specification used for prediction.
\item \code{target.study.names}: normalized target studies used for target-constrained prediction.
}
}
\description{
Generate held-out predictions from the overall, study-specific individual,
and pretrained transfer models stored in a \code{gptLasso} fit.
}
\examples{
# Gaussian prediction example
set.seed(1234)
sim_dat <- sim.gaussian.data()
x_train <- sim_dat$x_train
x_test <- sim_dat$x_test
ytest <- split(x_test$sample_metadata$Y, x_test$sample_metadata$study)

fit_gaussian <- gptLasso(
  x = x_train,
  alpha.ptlasso = 0.5,
  family = "gaussian",
  type.measure = "mse",
  nfolds = 3
)

pred_gaussian <- predict(
  fit_gaussian,
  xtest = x_test,
  ytest = ytest,
  target = NULL,
  type = "response"
)

names(pred_gaussian)
str(pred_gaussian$yhatpre)
pred_gaussian$errpre

# Binomial prediction example
set.seed(5678)
sim_dat_bin <- sim.binary.data()
x_train_bin <- sim_dat_bin$x_train
x_test_bin <- sim_dat_bin$x_test
ytest_bin <- split(x_test_bin$sample_metadata$Y, x_test_bin$sample_metadata$study)

fit_binomial <- gptLasso(
  x = x_train_bin,
  alpha.ptlasso = 0.5,
  family = "binomial",
  type.measure = "auc",
  nfolds = 3
)

pred_binomial <- predict(
  fit_binomial,
  xtest = x_test_bin,
  ytest = ytest_bin,
  type = "response"
)

pred_binomial$errpre
}
