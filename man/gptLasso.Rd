% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gptLasso.R
\name{gptLasso}
\alias{gptLasso}
\title{Fit the multistudy multiview gptLasso pipeline}
\usage{
gptLasso(
  x,
  alpha.ptlasso = 0.5,
  family = c("gaussian", "binomial"),
  type.measure = c("default", "mse", "auc", "deviance"),
  rho = seq(0, 1, length = 11),
  overall.lambda = c("lambda.1se", "lambda.min"),
  ind.lambda = c("lambda.1se", "lambda.min"),
  pre.lambda = c("lambda.1se", "lambda.min"),
  target = NULL,
  foldid = NULL,
  nfolds = 10,
  verbose = FALSE,
  fitoverall = NULL,
  fitind = NULL,
  penalty.factor = NULL,
  group.intercepts = TRUE,
  alpha.glmnet = 1,
  parallel = FALSE,
  ncores = 1L,
  ...
)
}
\arguments{
\item{x}{A named list with entries \code{feature_table}, \code{sample_metadata}, and
\code{feature_metadata}. The feature table must be feature-by-sample, the sample
metadata must contain \code{sample_id}, \code{study}, and \code{Y}, and the feature
metadata must contain \code{featureID} plus a view-mapping column such as
\code{featureType}.}

\item{alpha.ptlasso}{Transfer-learning level in \verb{[0, 1]}.}

\item{family}{Response family. Currently \code{"gaussian"} and \code{"binomial"} are supported.}

\item{type.measure}{Cross-validation metric optimized inside the multiview fits.}

\item{rho}{Multiview cooperative-learning fusion parameter, supplied as one value or a tuning grid.}

\item{overall.lambda}{Lambda rule used for the stage-one overall model.}

\item{ind.lambda}{Lambda rule used for the individual models.}

\item{pre.lambda}{Lambda rule used for the pretrained models.}

\item{target}{Optional target study/studies. Default \code{NULL} reproduces current
behavior and fits study-specific models for all studies. When provided,
must be a character vector of study names; \code{fitind}/\code{fitpre} are restricted
to these targets (in training-study order), while \code{fitoverall} is still fit
on all studies.}

\item{foldid}{Optional stacked fold assignment across all studies.}

\item{nfolds}{Number of folds used when \code{foldid} is not supplied.}

\item{verbose}{Should progress messages be printed?}

\item{fitoverall}{Optional pre-fit overall multiview model to reuse.}

\item{fitind}{Optional pre-fit list of study-specific multiview models to reuse.}

\item{penalty.factor}{Optional penalty factors across concatenated views.}

\item{group.intercepts}{Should study-specific stage-one baselines be used.}

\item{alpha.glmnet}{Elastic-net mixing parameter passed to the multiview base learner, default is 1 indicating Lasso regression.}

\item{parallel}{Logical; if \code{TRUE}, allow study-level parallel fits where available.}

\item{ncores}{Number of worker cores for study-level parallel fits.}

\item{...}{Additional arguments forwarded to the multiview base fitter.}
}
\value{
A \code{gptLasso} object, stored as a list. Important components include:
\itemize{
\item \code{study.names}: study labels detected from \code{x$sample_metadata$study}.
\item \code{view.names}: view labels detected from \code{x$feature_metadata}.
\item \code{n.by.study}: number of samples in each study.
\item \code{alpha.ptlasso}: the transfer-learning level used for the fit.
\item \code{fitoverall}: the pooled multiview stage-one fit.
\item \code{fitind}: a named list of study-specific individual fits.
\item \code{fitpre}: a named list of study-specific pretrained transfer fits.
\item \code{target}: the user-supplied target specification.
\item \code{target.study.names}: normalized target studies used for study-specific fitting.
\item \code{support.vars}: feature indices selected by the overall stage-one fit.
\item \code{group.baseline}: study-level baseline offsets used when \code{group.intercepts = TRUE}.
\item \code{preval.offset}: study-wise stage-one linear predictors reused as offsets in pretraining.
\item \code{training.layout}: the normalized training container split by study and view.
}
}
\description{
Fit the overall, study-specific individual, and pretrained transfer models
for a Bioconductor-style multistudy multiview input container.
}
\examples{
# Gaussian example
set.seed(1234)
sim_dat <- sim.gaussian.data()
x_train <- sim_dat$x_train
str(x_train)

fit_gaussian <- gptLasso(
  x = x_train,
  alpha.ptlasso = 0.5,
  family = "gaussian",
  type.measure = "mse",
  nfolds = 3
)

fit_gaussian_target <- gptLasso(
  x = x_train,
  alpha.ptlasso = 0.5,
  family = "gaussian",
  type.measure = "mse",
  target = "Study_2",
  nfolds = 3
)

names(fit_gaussian)
fit_gaussian$study.names
fit_gaussian$view.names
fit_gaussian$n.by.study
fit_gaussian$support.vars

# Binomial example
set.seed(5678)
sim_dat_bin <- sim.binary.data()
x_train_bin <- sim_dat_bin$x_train

fit_binomial <- gptLasso(
  x = x_train_bin,
  alpha.ptlasso = 0.5,
  family = "binomial",
  type.measure = "auc",
  nfolds = 3
)

names(fit_binomial)
fit_binomial$group.baseline
}
