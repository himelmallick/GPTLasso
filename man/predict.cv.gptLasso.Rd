% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.r
\name{predict.cv.gptLasso}
\alias{predict.cv.gptLasso}
\title{Predict from a cross-validated multistudy multiview gptLasso fit}
\usage{
\method{predict}{cv.gptLasso}(
  object,
  xtest,
  ytest = NULL,
  target = NULL,
  alpha.ptlasso = NULL,
  alpha.ptlasso.type = c("fixed", "varying"),
  type = c("link", "response", "class"),
  overall.lambda = c("lambda.min", "lambda.1se"),
  ind.lambda = c("lambda.min", "lambda.1se"),
  pre.lambda = c("lambda.min", "lambda.1se"),
  return.link = FALSE,
  ...
)
}
\arguments{
\item{object}{A fitted \code{cv.gptLasso} object.}

\item{xtest}{A named list with \code{feature_table}, \code{sample_metadata}, and \code{feature_metadata}.}

\item{ytest}{Optional list of response vectors used to compute performance summaries.}

\item{target}{Optional target study/studies for prediction. Default \code{NULL}
uses the target configuration stored in \code{object} (or all studies for legacy
fits). When supplied, \code{xtest} studies must be a subset of the resolved
target studies.}

\item{alpha.ptlasso}{Optional user-specified transfer-learning choice. May be one value or one per study.}

\item{alpha.ptlasso.type}{Either \code{"fixed"} or \code{"varying"} when \code{alpha.ptlasso} is not supplied.}

\item{type}{Prediction scale: \code{"link"}, \code{"response"}, or \code{"class"} for binomial fits.}

\item{overall.lambda}{Lambda rule used for overall-model prediction.}

\item{ind.lambda}{Lambda rule used for individual-model prediction.}

\item{pre.lambda}{Lambda rule used for pretrained-model prediction.}

\item{return.link}{Should link-scale predictions also be returned?}

\item{...}{Reserved for future extensions.}
}
\value{
A \code{predict.cv.gptLasso} object with the same prediction components as
\code{predict.gptLasso()}, plus:
\itemize{
\item \code{alpha.ptlasso}: the chosen transfer-learning value, either fixed or study-specific.
\item \code{target}: the effective target specification used for prediction.
\item \code{target.study.names}: normalized target studies used for target-constrained prediction.
\item \code{fit}: the originating \code{cv.gptLasso} object.
}
When \code{ytest} is supplied, the returned object also includes \code{metrics},
\code{erroverall}, \code{errind}, and \code{errpre}. Each metric table has rows \code{overall},
\code{ind}, and \code{pre}, and columns \code{pooled}, \code{mean}, plus one column per study.
}
\description{
Resolve one fixed or varying transfer-learning choice from a \code{cv.gptLasso}
object and generate held-out predictions from the corresponding overall,
individual, and pretrained models.
}
\examples{
# Gaussian cross-validated prediction example
set.seed(1234)
sim_dat <- sim.gaussian.data()
x_train <- sim_dat$x_train
x_test <- sim_dat$x_test
ytest <- split(x_test$sample_metadata$Y, x_test$sample_metadata$study)

cv_fit_gaussian <- cv.gptLasso(
  x = x_train,
  family = "gaussian",
  type.measure = "mse",
  alpha.ptlasso.list = c(0, 0.5, 1),
  nfolds = 3
)

pred_cv_gaussian <- predict(
  cv_fit_gaussian,
  xtest = x_test,
  ytest = ytest,
  alpha.ptlasso.type = "fixed",
  type = "response"
)

pred_cv_gaussian$alpha.ptlasso
pred_cv_gaussian$errpre

# Binomial cross-validated prediction example
set.seed(5678)
sim_dat_bin <- sim.binary.data()
x_train_bin <- sim_dat_bin$x_train
x_test_bin <- sim_dat_bin$x_test
ytest_bin <- split(x_test_bin$sample_metadata$Y, x_test_bin$sample_metadata$study)

cv_fit_binomial <- cv.gptLasso(
  x = x_train_bin,
  family = "binomial",
  type.measure = "auc",
  alpha.ptlasso.list = c(0, 0.5, 1),
  nfolds = 3
)

pred_cv_binomial <- predict(
  cv_fit_binomial,
  xtest = x_test_bin,
  ytest = ytest_bin,
  alpha.ptlasso.type = "fixed",
  type = "response"
)

pred_cv_binomial$fit$alpha.ptlasso.hat
}
