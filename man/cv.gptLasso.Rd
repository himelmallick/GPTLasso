% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.gptLasso.r
\name{cv.gptLasso}
\alias{cv.gptLasso}
\title{Cross-validate the multistudy multiview gptLasso pipeline}
\usage{
cv.gptLasso(
  x,
  alpha.ptlasso.list = seq(0, 1, length = 11),
  family = c("gaussian", "binomial"),
  type.measure = c("default", "mse", "auc", "deviance"),
  rho = seq(0, 1, length = 11),
  nfolds = 10,
  foldid = NULL,
  overall.lambda = c("lambda.min", "lambda.1se"),
  ind.lambda = c("lambda.min", "lambda.1se"),
  pre.lambda = c("lambda.min", "lambda.1se"),
  target = NULL,
  verbose = FALSE,
  fitoverall = NULL,
  fitind = NULL,
  group.intercepts = TRUE,
  parallel = FALSE,
  ncores = 1L,
  ...
)
}
\arguments{
\item{x}{A named list with \code{feature_table}, \code{sample_metadata}, and \code{feature_metadata}.}

\item{alpha.ptlasso.list}{Numeric vector of transfer-learning values to compare.}

\item{family}{Response family. Currently \code{"gaussian"} and \code{"binomial"} are supported.}

\item{type.measure}{Cross-validation metric used to compare transfer-learning levels.}

\item{rho}{Multiview cooperative-learning fusion parameter, supplied as one value or a tuning grid.}

\item{nfolds}{Number of folds used inside each \code{gptLasso()} fit.}

\item{foldid}{Optional stacked fold assignment across all studies.}

\item{overall.lambda}{Lambda rule used for the overall model when summarizing CV performance.}

\item{ind.lambda}{Lambda rule used for the individual models when summarizing CV performance.}

\item{pre.lambda}{Lambda rule used for the pretrained models when summarizing CV performance.}

\item{target}{Optional target study/studies. Default \code{NULL} reproduces current
behavior and evaluates all studies. When provided, must be a character
vector of study names; candidate pretrained performance and alpha selection
are summarized over these target studies.}

\item{verbose}{Should progress messages be printed?}

\item{fitoverall}{Optional pre-fit overall multiview model reused across alphas.}

\item{fitind}{Optional pre-fit list of study-specific multiview models reused across alphas.}

\item{group.intercepts}{Should study-specific stage-one baselines be used?}

\item{parallel}{Logical; if \code{TRUE}, allow study-level parallel fits where available.}

\item{ncores}{Number of worker cores for study-level parallel fits.}

\item{...}{Additional arguments forwarded to \code{gptLasso()}.}
}
\value{
A \code{cv.gptLasso} object, stored as a list. Important components include:
\itemize{
\item \code{alpha.ptlasso.hat}: the selected fixed transfer-learning value; when \code{target != NULL}, this value is selected by optimizing the target-study \code{mean} column of \code{errpre}.
\item \code{varying.alpha.ptlasso.hat}: target-specific transfer-learning values chosen from the same grid.
\item \code{alpha.ptlasso.list}: the candidate transfer-learning grid that was evaluated.
\item \code{errpre}: a matrix summarizing pretrained performance for each candidate alpha, with \code{pooled}, \code{mean}, and study-specific columns.
\item \code{target}: the user-supplied target specification.
\item \code{target.study.names}: normalized target studies used in CV summaries.
\item \code{errind}: performance summary for the individual study fits on the training layout.
\item \code{erroverall}: performance summary for the pooled stage-one fit on the training layout.
\item \code{fitoverall}: the shared overall fit reused across the alpha grid.
\item \code{fitind}: the shared list of individual study fits reused across the alpha grid.
\item \code{fit}: the list of \code{gptLasso} objects corresponding to \code{alpha.ptlasso.list}.
\item \code{overall.lambda}, \code{ind.lambda}, \code{pre.lambda}: lambda rules used when summarizing candidate performance.
}
}
\description{
Refit \code{gptLasso()} over a grid of transfer-learning values and summarize the
pretrained performance profile used to select the final \code{alpha.ptlasso}.
}
\examples{
# Gaussian cross-validation example
set.seed(1234)
sim_dat <- sim.gaussian.data()
x_train <- sim_dat$x_train

cv_fit_gaussian <- cv.gptLasso(
  x = x_train,
  family = "gaussian",
  type.measure = "mse",
  alpha.ptlasso.list = c(0, 0.5, 1),
  nfolds = 3
)

cv_fit_gaussian$alpha.ptlasso.hat
cv_fit_gaussian$varying.alpha.ptlasso.hat
cv_fit_gaussian$errpre

cv_fit_target <- cv.gptLasso(
  x = x_train,
  family = "gaussian",
  type.measure = "mse",
  target = c("Study_1", "Study_3"),
  alpha.ptlasso.list = c(0, 0.5, 1),
  nfolds = 3
)

# Binomial cross-validation example
set.seed(5678)
sim_dat_bin <- sim.binary.data()
x_train_bin <- sim_dat_bin$x_train

cv_fit_binomial <- cv.gptLasso(
  x = x_train_bin,
  family = "binomial",
  type.measure = "auc",
  alpha.ptlasso.list = c(0, 0.5, 1),
  nfolds = 3
)

cv_fit_binomial$alpha.ptlasso.hat
head(cv_fit_binomial$errpre)
}
